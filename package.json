{
  "name": "rentalcoverautomation",
  "version": "1.0.0",
  "description": "- Node.js (v16 or higher recommended)\r - npm (comes with Node.js)\r - Google Chrome, Firefox, or WebKit browsers (Playwright will install these automatically)\r - (Optional) Python 3.x and virtual environment if integrating with Python tools",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
